import { bibleBackground } from '../../../assets'

const QuoteBlockData = {
    backgroundImage: bibleBackground,
    source: 'Esther 4:14',
    quoteText: 'Who knows if perhaps you were made Queen for such a time as this?'
}

export default QuoteBlockData